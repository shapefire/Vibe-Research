# Vibe-Research Docker Compose 环境变量示例
# 用法：复制为同目录下的 .env，再启动或部署
#   cp docker/.env.example docker/.env
#
# deploy.sh 与 docker compose 会从 docker/ 目录加载此文件（见 docker-compose.yml env_file）

# ── 安全（公网部署必设） ─────────────────────────────────────────────
# CORS 白名单，逗号分隔。内网/本地可用 *；公网请改为你的域名：
#   VR_ALLOW_ORIGINS=https://your-domain.com
VR_ALLOW_ORIGINS=*

# API 鉴权密钥。设置后，除 /api/health 外所有 /api/* 需带 Authorization: Bearer <key>。
# 公网部署务必设置强随机值，例如：
#   VR_API_KEY=$(openssl rand -hex 32)
VR_API_KEY=

# ── 数据目录（一般无需修改） ─────────────────────────────────────────
# compose 已将 volume vr-data 挂载到 /data；此项与 compose environment 保持一致即可
# VR_DATA_DIR=/data

# ── 数据源（可选） ───────────────────────────────────────────────────
# 行情/研报等默认可用；仅 iwencai 语义搜索研报需要 key：
# IWENCAI_API_KEY=
# IWENCAI_BASE_URL=https://openapi.iwencai.com

# 代理：国内财经数据源默认直连。仅当服务器必须走代理出网时设为 1：
# VR_DATA_PROXY=1

# AI 配置无需在此填写；用户在网页「接入 AI」页配置（key 存浏览器本地）。
